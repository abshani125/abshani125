document.addEventListener('click',e=>{if(e.target.matches('[data-add]')){const c=Number(localStorage.getItem('ab-cart')||0)+1;localStorage.setItem('ab-cart',c);document.querySelectorAll('[data-cart]').forEach(x=>x.textContent=c);e.target.textContent='Added ✓';setTimeout(()=>e.target.textContent='Add to Bag',900)}});document.addEventListener('DOMContentLoaded',()=>{const c=localStorage.getItem('ab-cart')||0;document.querySelectorAll('[data-cart]').forEach(x=>x.textContent=c);});
